请从以下提供的数据中，建立精确的影像发现与诊断结论的直接映射关系。

解剖结构列表：
{structures}

病变特征列表：
{features}

诊断信息列表：
{diagnoses}

请遵循以下原则构建映射：
1. 影像发现应直接描述具体影像特征，不要添加"的特征"等多余后缀
2. 每个诊断应对应至少一个具体的影像发现，避免使用"综合影像表现"这类模糊描述
3. 映射应基于文本中明确的证据，而非推测

请按以下JSON格式返回结果：
[
  {
    "影像发现": "主动脉瓣钙化、增厚",  // 直接描述具体影像特征
    "对应诊断": "主动脉瓣钙化、增厚,开放受限",  // 对应的诊断
    "映射置信度": "高"  // 基于文本匹配的置信度评估
  }
]
