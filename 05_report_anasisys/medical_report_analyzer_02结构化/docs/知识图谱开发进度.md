# 医学影像报告知识图谱开发进度

## 已完成功能

1. **基础关系建模模块**
   - `RelationshipBuilder`类实现
   - 特征-诊断映射收集功能
   - 概率模型构建
   - 诊断指南提取
   - 双向模板生成

2. **关系建模用户界面**
   - 构建知识库功能
   - 查看关系模型可视化
   - 生成双向驱动模板

3. **知识库管理**
   - 保存/加载知识库
   - 基础数据可视化

## 开发计划

### 阶段一：知识图谱基础设施 (当前进行中)

- [ ] 设计知识图谱数据结构
  - 定义节点类型（诊断、特征、解剖部位）
  - 定义关系类型（提示、表现为、位于、共存）
  - 实现图数据结构

- [ ] 实现知识图谱构建功能
  - 从已有映射数据构建图结构
  - 添加节点和关系属性
  - 实现置信度计算

- [ ] Neo4j集成
  - 复用现有Neo4j代码
  - 实现图数据存储功能
  - 添加图数据查询接口

### 阶段二：知识图谱增强 (计划下周)

- [ ] 添加推理功能
  - 实现基于特征的诊断推理
  - 实现基于诊断的特征生成
  - 添加置信度评分机制

- [ ] LLM增强知识图谱
  - 利用LLM提取复杂关系
  - 从医学文献中提取知识
  - 知识图谱修正和验证

- [ ] 用户界面集成
  - 添加知识图谱构建UI
  - 实现知识图谱可视化
  - 添加交互式编辑功能

### 阶段三：预测模型实现 (计划两周后)

- [ ] 基础预测模型
  - 特征-诊断匹配模型
  - 置信度评分系统
  - 结果解释机制

- [ ] 交互式关系调整
  - 手动权重调整界面
  - 规则编辑器
  - 反馈收集机制

### 阶段四：系统集成与测试 (计划三周后)

- [ ] 报告生成集成
  - 将知识图谱与报告生成器集成
  - 实现规范化报告自动生成
  - 添加诊断依据和引用

- [ ] 系统测试
  - 准确性评估
  - 性能测试
  - 用户体验测试

## 实施细节

### 知识图谱数据结构

```python
# 节点类型
NODE_TYPES = {
    "DIAGNOSIS": "诊断",
    "FEATURE": "影像特征",
    "ANATOMY": "解剖部位"
}

# 关系类型
RELATION_TYPES = {
    "INDICATES": "提示",  # 特征提示诊断
    "EXHIBITS": "表现为",  # 诊断表现为特征
    "LOCATED_AT": "位于",  # 特征位于解剖部位
    "COEXISTS_WITH": "共存"  # 特征与特征共存
}
```

### 知识图谱基本类

```python
class MedicalKnowledgeGraph:
    def __init__(self):
        self.nodes = {}  # 存储节点（诊断和特征）
        self.relations = []  # 存储关系
        
    def add_node(self, id, type, properties=None):
        """添加节点"""
        pass
        
    def add_relation(self, from_id, to_id, type, properties=None):
        """添加关系"""
        pass
        
    def get_nodes(self, type=None):
        """获取节点"""
        pass
        
    def get_relations(self, from_id=None, to_id=None, type=None):
        """获取关系"""
        pass
```

### 关系构建与权重计算

```python
def build_knowledge_graph(self):
    """从已收集的特征-诊断对构建知识图谱"""
    pass
    
def _calculate_confidence(self, feature, diagnosis):
    """计算特征对诊断的置信度"""
    pass
```

### 推理功能

```python
def find_relevant_diagnoses(self, features):
    """根据输入的多个特征，找出最相关的诊断"""
    pass
    
def generate_features_for_diagnosis(self, diagnosis):
    """根据诊断生成典型特征描述"""
    pass
```

## 下一步具体任务

1. 实现`MedicalKnowledgeGraph`类
2. 扩展`RelationshipBuilder`类，添加知识图谱构建功能
3. 实现Neo4j存储和查询接口
4. 更新UI，添加知识图谱构建和可视化功能
